{% extends 'base.html.twig' %}

{% block title %} Shop {% endblock %}

{% block body %}
    <hr>
    {{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}